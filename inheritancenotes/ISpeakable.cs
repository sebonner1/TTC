namespace inheritancenotes
{
    public interface ISpeakable // INTERFACE keyword
    {
        public void SayHello();
        // This extends the classes so that the cat and people can say hello
        // We used IComparable
    }
}